# 物品、宠物与装备规则

## 物品分类

- `potion`：即时恢复体力，不进入装备栏。
- `gold`：即时增加金币，不进入背包。
- `weapon`：武器槽一次装备一件。
- `accessory`：按 `slot` 进入头部、背部、脸部或手部槽位。
- `skin`：有 `slot` 时按装备物品处理；无 `slot` 时作为角色外观收藏。
- `pet`：宠物蛋类奖品，解锁 `petId`，不进入装备背包。
- `surprise`：彩蛋或特殊道具，按自身效果处理。

奖池和职业掉落共同组成统一物品目录，物品通过 `id` 去重，重复装备会转化为金币。

## 可同时装备的物品

角色同时拥有 5 个装备位置：

| 槽位 | 规则 |
| --- | --- |
| weapon | 同时一件武器，换武器会替换当前武器 |
| head | 同时一件头部装备 |
| back | 同时一件背部装备 |
| face | 同时一件脸部装备 |
| hand | 同时一件手部装备 |

头、背、脸、手四个槽位的属性加成会叠加计算。旧存档中的 `equipment.accessory` 会迁移到 `hand`，旧版 `equipment.accessories` 也会被读取。

## 宠物解锁

- 绿史莱姆：初始赠送。
- 瞌睡黑猫：抽到黑猫玩偶后解锁。
- 火元素宝宝：抽到火晶蛋后解锁。
- 幽灵灯：抽到幽灵灯笼后解锁，黑市商品价格额外降低 5%。
- 幼龙：完成任务总数达到 100 后自动解锁。
- 像素虫：在任务咒语中输入“上上下下”“konami”或“像素虫”触发彩蛋。

当前出战宠物只能有一只，但宠物所有权会永久保存在 `pets` 数组中。宠物点击互动、被动效果和宠物切换不影响装备槽。

## 彩蛋与特殊效果

- 像素虫：隐藏咒语触发后出现并自动出战。
- 一块砖头：惊喜奖池特殊物品。
- 黑猫、火元素、幽灵灯、幼龙分别对应经验、金币、商店和属性成长玩法。
- 抽奖重复的装备或宠物会转化为金币，不会覆盖已有收藏。

## 兼容策略

- 保留原有 `inventory` ID 数组，不破坏旧存档。
- 通过 `normalizeEquipment()` 将旧版装备字段归一化。
- 新增物品只需要加入 `ITEM_POOL`，不再单独维护多个查找逻辑。
